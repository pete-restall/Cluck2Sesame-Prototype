HEX_FILENAME=Test.hex
OBJ_FILES=\
	$(FIRMWARE_DIR)/Main.o \
	Test.o

all: $(HEX_FILENAME)

clean:
	$(RM) -f $(BUILD_ARTEFACT_PATTERN)

$(HEX_FILENAME): $(FIRMWARE_CORE_OBJ_FILES) $(OBJ_FILES)
	$(LD) $(LDARGS) -o $(HEX_FILENAME) $(FIRMWARE_CORE_OBJ_FILES) $(OBJ_FILES)

include $(MAKEFILE_RULES)
